import styled from 'styled-components/native';

export const PageFilter = styled.SafeAreaView`
  background-color: #151515;
  flex: 1;
  padding: 3px 0;
`;
export const HeroesList = styled.FlatList`
  margin-bottom: 65px;
`;
