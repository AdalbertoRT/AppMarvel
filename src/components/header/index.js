import React from 'react';
import Logo from '../logo';
import {HeaderView} from './styles';

const Header = () => {
  return (
    <HeaderView>
      <Logo />
    </HeaderView>
  );
};

export default Header;
