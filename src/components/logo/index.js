import React from 'react';
import {LogoView, LogoText} from './styles';

const Logo = () => {
  return (
    <LogoView>
      <LogoText>MARVEL</LogoText>
    </LogoView>
  );
};

export default Logo;
